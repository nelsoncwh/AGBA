apiVersion: v1
kind: Service
metadata:
  name: aop-be-app-cv1
  namespace: __APP_ENV__
  labels:
    app: aop-be-app-cv1
    team: spring-boot-actuator
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: aop-be-app-cv1
    team: spring-boot-actuator