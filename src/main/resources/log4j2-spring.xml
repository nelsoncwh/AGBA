<?xml version="1.0" encoding="UTF-8"?>
<Configuration>
  <Properties>
    <property name="logFileName">${spring:logging.file.name}</property>
    <property name="logFilePath">${spring:logging.file.path}</property>
  </Properties>
  <Appenders>
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout
        pattern="%style{%d{ISO8601}}{black} %highlight{%-5level }[%style{%t}{bright,blue}] %style{%C{1.}}{bright,yellow}: %msg%n%throwable"/>
    </Console>

    <RollingFile name="RollingFile"
      fileName="${logFilePath}/${logFileName}.log"
      filePattern="${logFilePath}/$${date:yyyy-MM}/${logFileName}-%d{yyyy-MM-dd}-%i.log.gz">
      <PatternLayout>
        <pattern>%d{ISO8601_BASIC} %p [%t] %C{1.}: %m%n</pattern>
      </PatternLayout>
      <Policies>
        <!-- rollover on startup, daily and when the file reaches
            10 MegaBytes -->
        <OnStartupTriggeringPolicy/>
        <SizeBasedTriggeringPolicy size="10 MB"/>
        <TimeBasedTriggeringPolicy/>
      </Policies>
    </RollingFile>

    <RollingRandomAccessFile name="AccessLog"
      fileName="${logFilePath}/${logFileName}-access.log"
      filePattern="${logFilePath}/$${date:yyyy-MM}/${logFileName}-access-%d{MM-dd-yyyy}-%i.log.gz"
      append="true">
      <PatternLayout>
        <pattern>[%t] %d{dd MM yyyy HH:mm:ss,SSS} %-5p %-15c{1} [%X]: %m%n</pattern>
      </PatternLayout>
      <Policies>
        <!-- rollover on startup, daily and when the file reaches
            10 MegaBytes -->
        <OnStartupTriggeringPolicy/>
        <SizeBasedTriggeringPolicy size="10 MB"/>
        <TimeBasedTriggeringPolicy/>
      </Policies>
    </RollingRandomAccessFile>

  </Appenders>

  <Loggers>
    <logger name="reactor.netty.http.server.AccessLog" level="INFO" additivity="false">
      <AppenderRef ref="AccessLog"/>
    </logger>
    <!-- LOG everything at INFO level -->
    <Root level="info">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="RollingFile"/>
    </Root>
  </Loggers>

</Configuration>